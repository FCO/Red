name: linux

on:
  - push

jobs:
  perl6:

    runs-on: ubuntu-latest

    container:
      image: fernandocorrea/red-tester-no-run

    steps:
      - uses: actions/checkout@v1
      - name: perl6 -v
        run: perl6 -v
      - name: Run Tests
        run: zef install -v .
